# Root shim — Python app lives in apps/python/. Install with:
#   pip install -r apps/python/requirements.txt
# This file keeps `pip install -r requirements.txt` working from the repo root.
-r apps/python/requirements.txt
